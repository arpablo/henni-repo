spring:
  jackson:
    serialization:
      indent-output: true
  mvc:
    favicon:
      enabled: false
  thymeleaf:
    mode: HTML
  profiles:
    active: @spring.profiles.active@
logging:
    level:
        ROOT: INFO
        de.arpablo: DEBUG
    pattern:
        console: "%-5level %d{yyyy-MM-dd HH:mm:ss} %logger{36} - %msg%n"
henni:
  repo:
    basedir: ~/repo